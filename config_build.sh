#!/bin/bash

# Build config for the build script, build.sh. Look there for more info.

APP_NAME=nicolivehelperadvance
CHROME_PROVIDERS="chrome"
CLEAN_UP=1
ROOT_FILES=""
ROOT_DIRS="components defaults libs modules"
BEFORE_BUILD=
AFTER_BUILD=
